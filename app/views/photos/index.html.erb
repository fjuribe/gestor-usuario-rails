<% content_for :titiel do %>
Un listado de fotos 
<% end %>

<h1><%= t('.title') %></h1>


<% @photos.each do |photo| %>
     <%= link_to photo_path(photo.id) do %>
          <img src="<%= photo.url %>" alt="<%= photo.name %>" height="180" class="img-rounded">
     <% end %>
<% end %>